/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import ContentContainer from './ContentContainer';

storiesOf('ContentContainer', module).add('default', () => <ContentContainer />);
